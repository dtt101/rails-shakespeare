<% if @performance.errors.any? %>
	$("#performance_errors").append("<ul>");
	<% @performance.errors.full_messages.each do |msg| %>
		$("#performance_errors").append("<li><%= msg %></li>");
	<% end %>
	$("#performance_errors").append("</ul>");
<% else %>
	$.colorbox.close();
	// TODO: show line is partial
<% end %>
